# OS noise
.DS_Store

# Salamander samples — fetched by the loop in the README, ~4 MB, not ours to commit
tools/samples/*.mp3

# Full-length source recordings for the human excerpts (archive.org URLs in README)
tools/source-human/

# Render intermediates
audio/**/*.wav

# Local Netlify folder
.netlify

# Function dependencies (installed at deploy time from the root package.json;
# netlify/functions/package.json remains for local CLI workflows)
node_modules
netlify/functions/node_modules
